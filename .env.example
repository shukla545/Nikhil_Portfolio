# Optional: set this only if you deploy the assistant backend separately.
# Leave blank on Vercel to use the built-in Next.js /api/chat route.
NEXT_PUBLIC_API_URL=

# Optional: enables real OpenAI streaming answers in the assistant API.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# Optional contact automation. Without these, the site uses mailto fallback.
OWNER_EMAIL=nikhilshukla8652@gmail.com
